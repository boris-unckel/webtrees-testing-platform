<?xml version="1.0" encoding="UTF-8"?>
<!-- SPDX-License-Identifier: AGPL-3.0-or-later -->
<!--
  Teststufe 2 — Komponentenintegrationstest (PHPUnit + MySQL)
  Testet webtrees mit echter MySQL-Datenbank
-->
<phpunit xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:noNamespaceSchemaLocation="https://schema.phpunit.de/12.0/phpunit.xsd"
         bootstrap="/tests/layer3-integration/bootstrap.php"
         cacheDirectory="/tmp/phpunit-cache"
         colors="true"
         failOnRisky="true"
         failOnWarning="true"
         beStrictAboutTestsThatDoNotTestAnything="true">

    <testsuites>
        <testsuite name="integration">
            <directory>/tests/layer3-integration/tests</directory>
        </testsuite>
    </testsuites>

    <php>
        <env name="MYSQL_HOST" value="mysql"/>
        <env name="MYSQL_PORT" value="3306"/>
        <env name="MYSQL_DATABASE" value="webtrees_test"/>
        <env name="MYSQL_USER" value="webtrees"/>
        <env name="MYSQL_PASSWORD" value="webtrees_test"/>
        <!-- R4 (Phase K): PHPUnit-interner PHP-Prozess. display_errors=On druckt
             Errors einmal auf stderr; log_errors=On würde zusätzlich (über die
             stderr-Fallback-Semantik im PHPUnit-Kontext) eine zweite Kopie mit
             "PHP "-Präfix ausgeben. Wir unterdrücken log_errors scoped auf
             PHPUnit, damit Deprecations/Warnings nur einmal im stdout-Log
             landen. Der CLI-Fall wird separat im Containerfile gelöst
             (/var/log/php_errors.log). -->
        <ini name="log_errors" value="0"/>
    </php>

    <source>
        <include>
            <directory suffix=".php">/var/www/html/app</directory>
        </include>
    </source>

    <coverage>
        <report>
            <clover outputFile="/coverage/layer3-coverage.xml"/>
        </report>
    </coverage>
</phpunit>
